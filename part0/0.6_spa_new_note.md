# Exercise 0.6 - New note in Single page app diagram

```mermaid
sequenceDiagram
participant browser
participant server

Note right of browser: User types note and clicks "Save"

browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa<br>with JSON data (new note content)
activate server
server-->>browser: HTTP status code 201 with message {"message":"note created"}
deactivate server

Note right of browser: Browser executes the event handler to render notes to display including newly added note (without reloading the page)


```
